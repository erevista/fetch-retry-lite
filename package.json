{
  "name": "fetch-retry-lite",
  "version": "0.1.0",
  "description": "Lightweight fetch retry with exponential backoff and full jitter. Zero dependencies.",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.mts",
        "default": "./dist/index.mjs"
      },
      "require": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      }
    }
  },
  "files": [
    "dist"
  ],
  "keywords": [
    "fetch",
    "retry",
    "backoff",
    "exponential-backoff",
    "jitter",
    "fetch-retry",
    "network",
    "resilience"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/erevista/fetch-retry-lite"
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
